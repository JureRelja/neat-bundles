import { BundleBuilder } from "./bundle_builder";

export class BundleBuilderConfig {
    id: number;

    skipTheCart: boolean;

    allowBackNavigation: boolean;

    showOutOfStockProducts: boolean;

    bundleBuilderId: number;

    BundleBuilder: BundleBuilder;
}
