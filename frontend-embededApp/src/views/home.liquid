{% assign people = "alice, bob, carol" | split: ", " -%}

<h1>{{ name }}</h1>
<ul>
{%- for person in people %}
  <li>
    <a href="{{person | prepend: "https://example.com/"}}">
      {{ person | capitalize }}
    </a>
  </li>
{%- endfor%}
</ul>
